# Backend base URL (only needed if you don't use the Vite proxy)
VITE_API_BASE_URL=http://localhost:8000

# Optional: turn on/off auth UI (if you ever want to ship a “no-auth demo” mode)
VITE_REQUIRE_AUTH=true
